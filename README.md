# Aruna PV Dashboard

Password-protected static dashboard. Open `index.html` on the deployed site and enter the access code.
All page payloads are stored encrypted (AES-256-GCM); this repository contains no readable dashboard data.
